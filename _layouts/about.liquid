---
layout: default
---

<div class="post">
  <header class="post-header">
    <div class="d-flex align-items-center">
      <!-- Profile Image -->
      {% if page.profile.image %}
        <div class="profile float-left" style="margin-right: 20px;">
          {% assign profile_image_path = page.profile.image | prepend: 'assets/img/' %}
          <img src="{{ profile_image_path }}" alt="Profile image" class="img-fluid profile-img" style="height: 200px; object-fit: cover; border-radius: 10px;">
        </div>
      {% endif %}

      <!-- Name and Basic Info -->
      <div class="col-sm-9">
        <h1 class="post-title">
          {{ site.first_name }} {{ site.last_name }}
        </h1>
        <p class="desc">
          {{ site.current_status }}<br>
          {{ site.affiliation }}<br>
          {{ site.office }}<br>
          {{ site.email_contact }}
        </p>

        <!-- Social Icons -->
        <div class="social-small">
          <div class="contact-icons">
            <a href="mailto:{{ site.email | encode_email }}" title="Email"><i class="fas fa-envelope"></i></a>
            <span class="divider" style="margin: 0 5px;"></span>
            {% if site.scholar_userid %}
              <a href="https://scholar.google.com/citations?user={{ site.scholar_userid }}" title="Google Scholar"><i class="ai ai-google-scholar"></i></a>
              <span class="divider" style="margin: 0 5px;"></span>
            {% endif %}
            {% if site.github_username %}
              <a href="https://github.com/{{ site.github_username }}" title="GitHub"><i class="fab fa-github"></i></a>
            {% endif %}
          </div>
        </div>
      </div>
    </div>
  </header>

  <article>
    <div class="clearfix">{{ content }}</div>

    {% if page.news and site.announcements.enabled %}
      <h2>
        <a href="{{ '/news/' | relative_url }}" style="color: inherit">🔥 News</a>
      </h2>
      {% include news.liquid limit=true %}
    {% endif %}

    {% if site.latest_posts.enabled %}
      <h2>
        <a href="{{ '/blog/' | relative_url }}" style="color: inherit">📝 Latest posts</a>
      </h2>
      {% include latest_posts.liquid %}
    {% endif %}

    {% if page.selected_papers %}
      <h2>
        <a href="{{ '/publications/' | relative_url }}" style="color: inherit">📃 Selected publications</a>
      </h2>
      {% include selected_papers.liquid %}
    {% endif %}

    {% if page.social %}
      <div class="social">
        <div class="contact-icons">{% include social.liquid %}</div>
        <div class="contact-note">{{ site.contact_note }}</div>
      </div>
    {% endif %}

    {% if site.newsletter.enabled and site.footer_fixed %}
      {% include scripts/newsletter.liquid center=true %}
    {% endif %}
  </article>
</div>
